---
title: nope
layout: blog
---

<%# This one vvv is for a most-recent panel! %>
<%# <% value = %x(ls -t -1 source/thoughts| head -5) %>
<%# This one though vvvv is for  %>
<%# <% value = %x(ls -l source/thoughts | sed -e "s/  */ /g" | awk '{if (NR!=1) {print}}' | cut -d " " -f 6-) %>
<%# <% value.split("\n").each do |data|%>
<%#     <% metadata = data.split(" ")%>
<%#     <% for i in metadata.length%>
<%#         <%= puts i %>
<%#     <% end %>

<%# <% value = %x(ls -t -1 source/thoughts| head -5) %>
<% value = %x(ls -t -1 source/thoughts) %>
<% value.split("\n").each do |filename|%>
    <% filename = File.basename(filename, '.*') %>
    <% filename = File.basename(filename, '.*') %>
<%= "[#{filename.gsub('-',' ').split.map(&:capitalize).join(' ')}](#{filename}.html)\n" %>
<% end %>
