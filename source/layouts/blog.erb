<% wrap_layout :layout do %>
    <!--
    <aside>
        Andres Cuervo
        <% recent_count = 5%>
        <% number = ( blog.articles.length < recent_count) ? blog.articles.length : recent_count %>

        <% unless number < 1 %>
            <h4><%= number %> Most Recent Posts!</h4>
            <hr>

            <% blog.articles[0...number].each do |article| %>
                <article class="article-sample" style="background-color: #222; padding: 0.2em 0.4em; border-radius: 10px; witdth: 100%">
                    <h5>
                        <a style="float: right" href="<%= article.url %>"><%= article.title %></a>
                        <br>
                        <time style="float: right" ><%= article.date.strftime('%b %e %Y') %></time>
                    </h5>

                </article>
            <% end %>
        <%end%>
    </aside> -->
    <article>
        <h1 class="blog-title"><%= current_page.data.title %></h1>
        <h3 class="blog-description"><%= current_page.data.description%></h3>
        <h3 class="blog-tags">tags: <%= current_page.data.tags %></h3>
        <div class="blog-date">
            <%= current_article && current_article.date ?
                current_article.date.strftime('%b %e, %Y') : '' %>
        </div>
        <hr>
        <%= yield %>
        <br/>
        <span style="float: right;">
            — Andres Cuervo
        </span>
    </article>
</section>
<section class="social-stuff">
    <h1 style="display: inline;">Want to share this post?</h1>
    <%= partial "social-buttons"%>
</section>
<!-- <section id="comments"> -->
<!--     <%# <%= partial "disqus&#45;thread"%> -->
<!-- </section> -->
<% end %>
